# Retired M6 experiment matrix

The one-variable 135M overlay experiments were retired when LaughLM adopted
the standalone 127M production configuration. Their YAML files are deliberately
not retained. Use `configs/production/laughlm_v1_127m_4b.yaml` for all current
training and resume work.
