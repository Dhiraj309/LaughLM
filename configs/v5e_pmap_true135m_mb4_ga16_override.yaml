# M6 constant-effective-token geometry experiment.
# Base: v5e_pmap_true135m_production.yaml

runtime:
  micro_batch_per_device: 4
  gradient_accumulation: 16
  checkpoint_dir: checkpoints/experiments/135M_mb4_ga16

optimizations:
  compilation_cache_dir: jax_cache/v5e_true135m_mb4_ga16

profiling:
  capture_device_memory_profile: true
  memory_profile_step: 10
  output_dir: profiles/135M_mb4_ga16
