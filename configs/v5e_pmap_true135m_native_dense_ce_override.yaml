# M7 native dense-CE comparison overlay.
# Base: v5e_pmap_true135m_production.yaml

loss:
  backend: native
  chunked_logits: false

runtime:
  checkpoint_dir: checkpoints/experiments/135M_native_dense_ce

optimizations:
  compilation_cache_dir: jax_cache/v5e_true135m_native_dense_ce
