# M7 Grain data-loader experiment overlay.
# Base: v5e_pmap_true135m_production.yaml
# Use only if native memmap input timing is proven to be the bottleneck.

optimizations:
  data_backend: grain
  compilation_cache_dir: jax_cache/v5e_true135m_grain

runtime:
  checkpoint_dir: checkpoints/experiments/135M_grain
