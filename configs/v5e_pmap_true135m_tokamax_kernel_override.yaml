# M7 Tokamax fused-kernel experiment overlay.
# Base: v5e_pmap_true135m_production.yaml
# Unsupported TPU kernels must use the documented native fallback.

optimizations:
  kernel_backend: tokamax
  compilation_cache_dir: jax_cache/v5e_true135m_tokamax_kernel

runtime:
  checkpoint_dir: checkpoints/experiments/135M_tokamax_kernel
