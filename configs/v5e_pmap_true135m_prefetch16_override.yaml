# M6 host-prefetch experiment overlay.
# Keep the production model/data/cache settings unchanged; vary only queue depth.

runtime:
  prefetch_size: 16
  checkpoint_dir: checkpoints/experiments/135M_prefetch16
