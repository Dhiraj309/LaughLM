# M7 Tokamax linear CE experiment overlay.
# Base: v5e_pmap_true135m_production.yaml
# TPU validation is required before treating this path as supported.

loss:
  backend: tokamax_linear_ce
  tokamax_implementation: mosaic_tpu
  chunked_logits: true
  z_loss: 0.0

runtime:
  checkpoint_dir: checkpoints/experiments/135M_tokamax_linear_ce

optimizations:
  compilation_cache_dir: jax_cache/v5e_true135m_tokamax_linear_ce
