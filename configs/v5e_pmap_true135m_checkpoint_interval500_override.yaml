# M6 isolated checkpoint-interval experiment.
# Base: v5e_pmap_true135m_production.yaml

runtime:
  checkpoint_interval: 500
  checkpoint_dir: checkpoints/experiments/135M_checkpoint_interval500

optimizations:
  compilation_cache_dir: jax_cache/v5e_true135m_checkpoint_interval500
