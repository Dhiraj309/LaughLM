# M6 isolated chunked-logits experiment.
# Base: v5e_pmap_true135m_production.yaml

loss:
  logits_chunk_size: 2048

runtime:
  checkpoint_dir: checkpoints/experiments/135M_logits2048

optimizations:
  compilation_cache_dir: jax_cache/v5e_true135m_logits2048
