# M5 bounded GQA stability overlay.
# Apply on top of v5e_pmap_true135m_production.yaml.

model:
  num_kv_heads: 4

architecture:
  attention_variant: gqa

runtime:
  checkpoint_dir: checkpoints/experiments/135M_gqa4_stability

optimizations:
  compilation_cache_dir: jax_cache/v5e_true135m_gqa4_stability
