# M6 isolated chunked-logits experiment.
# Base: v5e_pmap_true135m_production.yaml

loss:
  logits_chunk_size: 8192

runtime:
  checkpoint_dir: checkpoints/experiments/135M_logits8192

optimizations:
  compilation_cache_dir: jax_cache/v5e_true135m_logits8192

profiling:
  capture_device_memory_profile: true
  memory_profile_step: 10
  output_dir: profiles/135M_logits8192
